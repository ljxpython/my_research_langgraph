# Frontend Contract Assets

这个目录放“前端实现对齐资产”（不放业务代码），用于减少前后端对接阻碍。

建议内容：
- Workbench 页面布局与组件树（可视化 demo）
- AG-UI event store 的 state shape（示例 JSON）
- 关键交互流程（busy/取消/interrupt/resume）的示例

与 `shared/contracts/http/` 的区别：
- http/：接口契约与 JSON 示例
- frontend/：前端实现层的对齐资产
